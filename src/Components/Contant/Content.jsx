import { useLoaderData } from "react-router-dom";

const Content = () => {
    const content = useLoaderData()



    return (
        <div>
           <h1>Shahil</h1>
        </div>
    );
};

export default Content;